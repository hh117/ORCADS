request:
  robot:
    - which robots are available *for_that?
    - what robots are available?
    - which robots are available to send to <object>?
    - do we have a robot that <skill>?
    - do we have a robot <distance>?
  inspection:
    - the <object> needs to be inspected
    - <object> is going to be inspected
    - <object> needs inspection
  robot_type:
    - i want a <robot_type> robot
  send:
    - send <robot>
    - return <robot>
    - move <robot>
    - add_on:
      - to <object>
      - to <object> where <object> is located
      - <refexpr> *of <object>
      - <refexpr> *of <object> in <object>
      - <refexpr> *of <robot>
    - intro:
      - can you
  lookup:
    - take a picture of <object>
    - take a picture of <object>
    - intro:
      - can *<robot>
      - <robot>
  focus:
    - move <capdev> to <object>
    - move <robot> <capdev> to <object>
    - point <robot> <capdev> to <object>
    - place <robot> <capdev> <ref_exp> <object>
  video:
    - start:
      - stream video
      - send *the video
      - can you activate <capdev> *on_<robot>
    - close:
      - close <capdev>
      - close <robot> <capdev>
  start_water:
    - turn on the water *on
    - add_on:
      - <robot>

